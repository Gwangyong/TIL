# 2025.05.27(화) 컨벤션, Guard, Closure
> **Status**: 🎯 **블로그 발행**<br>**Blog**: [guard문과 Closure](https://jud00.tistory.com/entry/0527-%ED%99%94-TIL)

## Swift Convention Guide

컨벤션이랑 코드를 작성할 때 지키면 좋은 **통일된 규칙**을 의미함

Swift는 기본적으로 이름을 표기할 때 `Camel Case`로 이름을 만들고 있다.

이름을 지을 때 공백(스페이스바)를 넣을 수 없기 때문에, 공백 대신 그 다음 글자를 **대문자**로 쓴다.

```swift
my name is mori // Camel Case로 변경하면 아래처럼 된다.
myNameIsMori // 공백을 없애고 공백 다음에 첫 글자를 대문자로 변경
```

Snake Case도 있지만, Swift에서는 잘 사용하지 않는다.

공백을 _(언더스코어)로 변경하여, 뱀처럼 길게 이어진 모양이라서 `Snake case` 라고한다.

```swift
my name is mori // Snake Case로 변경하면 아래처럼 된다.
my_name_is_mori // 공백을 _로 변경
```

## Guard

- `guard` 는 조건이 `false` 일 때 코드를 더 이상 실행시키지 않고 코드 블록을 빠져나가는 조건문이다.
- 조건이 `true` 일 경우 코드를 계속 진행하고, `false`일 경우 `else`코드 블록에서 탈출해야한다.
- `else` 블록은 필수이다.

```swift
guard 조건 else {
	// 조건이 false 일 때 실행되는 코드블록
	// return, break, continue, throw등 코드 블록을 종료하는 구문이 필요함
	return
}

// 만약 위 조건이 false라면 아래 코드는 실행되지 않는다.
// ealry exit (빠른 탈출)이라고도 불린다.

// 조건이 true이면 아래의 코드를 계속 진행한다.
```

> 반복문 예시

```swift
for i in 0...10 {
	print(i)
	guard i < 5 else { return } // i가 5 이상이 되면 guard문에 걸려 return됨 → 함수 탈출
}

/* 출력 값
0
1
2
3
4
5
*/
```

### `if` vs `guard`

---

- `if` 조건문은 `true`일 때 코드블록을 실행하고, false 일 때 다른 처리를 할 수 있다.
- `guard`조건문은 `false` 일 때 즉시 탈출하는 방식이다.
- `if` 조건문은 코드블록을 명시적으로 작성해야 하지만, `guard`는 `true`일 때 코드블록이 없고 계속 진행한다.

## 클로저(Closure)

- 클로저는 `익명 함수` 라고도 불리며, **특정 작업을 수행하는 코드 블럭**이다.
- Swift에서 함수는 이름이 있는 클로저로 간주되며, 클로저는 함수보다 더 넓은 개념이다.
    
    <img width="1024" height="434" alt="Image" src="https://github.com/user-attachments/assets/6efb1a57-3118-4f35-a746-95903ba2f2cf" />
    
- 클로저는 함수의 4가지 요소(`이름`, `파라미터`, `반환타입`, `구현부`)에서 `이름`을 제외한  `파라미터`, `반환타입`, `구현부` 로 구성되어 있다.
- 클로저는 `데이터 타입`처럼 사용이 가능하다.
    - 변수/상수에 저장하여 재사용
    - 함수의 파라미터로 전달
    - 반환값으로 사용

### 클로저 타입 표기

---

```swift
// 타입으로 사용할 때 표현 방법
(파라미터 타입) -> 반환 타입
```

- 파라미터가 없다면 `()`를 작성해야 한다.
- 반환 타입이 없다면 `Void`를 명시하거나 생략할 수 있다.
- 클로저 타입은 옵셔널로도 선언이 가능하며, 전체를 괄호로 감싸고 `?`를 붙여야 한다.

> 예시
> 

```swift
let closure: () -> Void        // 파라미터와 반환 타입 없음
let closure2: (Int) -> Void    // Int를 받고 반환 없음
let closure3: (Int) -> Int     // Int를 받고 Int 반환
let closure4: ((Int) -> Int)?  // 옵셔널 클로저
```

### 클로저 구현 방법

---

- 클로저는 익명함수 이므로, 구현할 때 `in` 키워드를 사용하여 파라미터와 구현부를 구현해야함

```swift
{ (파라미터) in 
	// 구현부
	// 반환
}
```

- `in` 키워드를 사용하여 앞에는 파라미터 이름을, 뒤에는 구현부를 구분하여 작성
- 파라미터 이름은 자유롭게 작성하며, 생략도 가능함
- 생략하는 경우, `$0`, `$1`, `$2` 와 같은 위치 기반 인자를 사용할 수 있으며, 이럴 경우 `in` 키워드도 같이 생략해야한다.

```swift
let multiply = { (a: Int, b: Int) in 
	return a * b
}
```

### 클로저 사용(호출)

---

- 변수나 상수에 저장된 클로저는 함수처럼 호출한다.
- 옵셔널 클로저는 `변수 이름?()` 방식으로 호출한다.

> 일반 클로저 저장 및 실행
> 

```swift
let greet: () -> Void = {
  print("Hello")
}
greet() // Hello
```

> 옵셔널 클로저 저장 및 실행
> 

```swift
let optionalGreet: (() -> Void)? = {
  print("Hi")
}
optionalGreet?() // Hi
```

> 구현하고 바로 호출
> 

```swift
let hello = {
	return "안녕! 구현하자마자 호출!"
}()
print(hello) // 안녕! 구현하자마자 호출!
```

### 트레일링 클로저 (Trailing Closure)

---

- 트레일링 클로저는 **함수의 마지막 매개변수가 클로저**일 때, 괄호 바깥에 클로저를 작성할 수 있는 문법이다.
- 클로저가 길어질 때 가독성을 높이는데 유용하다.

```swift
// 기본 형태
func performTask(title: String, action: () -> Void) {
	print("Title: \(title)")
	action()
}

// 일반 방식
performTask(title: "Start", action: {
	print("Running task")
})

// 트레일링 클로저(Trailing Closure) 방식
performTask(title: "Start") {
	print("Running task")
}
```